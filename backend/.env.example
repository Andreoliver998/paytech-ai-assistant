# PayTechAI_Senior — backend/.env.example
#
# Segurança:
# - NÃO coloque segredos reais aqui.
# - Copie este arquivo para backend/.env e preencha localmente.
# - O arquivo backend/.env deve ficar fora do Git (ver .gitignore na raiz).

# =============================
# Obrigatório
# =============================
# Chave da OpenAI (não comitar)
OPENAI_API_KEY=

# =============================
# Opcionais (use somente se precisar sobrescrever defaults do código)
# =============================
# Ambiente: dev | prod
# ENV=dev

# Modelos / timeout
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_EMBED_MODEL=text-embedding-3-small
# OPENAI_TIMEOUT=60

# Auth/JWT
# JWT_SECRET=troque-este-segredo-em-producao
# JWT_ALG=HS256
# JWT_EXPIRES_MIN=720
# SEED_DEMO=0

# Diretórios/DB (persistência local)
# PAYTECH_DATA_DIR=./data
# PAYTECH_DB_URL=sqlite:///./data/paytech.db

# CORS (separado por vírgula)
# CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,http://127.0.0.1:8000,http://localhost:8000

# Chat / janela de contexto
# MAX_WINDOW_MESSAGES=20
# MAX_TOTAL_MESSAGES=80
# MAX_CHARS_PER_MESSAGE=4000

# RAG
# RAG_TOP_K=6
# RAG_CHUNK_SIZE=1200
# RAG_CHUNK_OVERLAP=200
# KB_PREVIEW_LIMIT=5000
# DOWNLOADS_CHUNK_TOKENS=700
# DOWNLOADS_CHUNK_OVERLAP_TOKENS=120

# Temperaturas
# TEMPERATURE_GENERAL=0.6
# TEMPERATURE_RAG=0.3
